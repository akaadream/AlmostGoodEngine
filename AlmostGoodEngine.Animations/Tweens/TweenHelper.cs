using Microsoft.Xna.Framework;

namespace AlmostGoodEngine.Animations.Tweens
{
    public static class TweenHelper
    {
        //public static T Lerp<T>(T start, T end, float amount) where T : struct
        //{
        //    if (typeof(T) == typeof(Color))
        //    {
        //        return Color.Lerp(start, end, amount);
        //    }

        //    if (typeof(T) == typeof(Color))
        //    {
        //        return Vector2.Lerp(start, end, amount);
        //    }

        //    if (typeof(T) == typeof(Color))
        //    {
        //        return Vector3.Lerp(start, end, amount);
        //    }

        //    if (typeof(T) == typeof(Color))
        //    {
        //        return Vector4.Lerp(start, end, amount);
        //    }

        //    if (typeof(T) == typeof(Color))
        //    {
        //        return Quaternion.Lerp(start, end, amount);
        //    }

        //    if (typeof(T) == typeof(Color))
        //    {
        //        return float.Lerp(start, end, amount);
        //    }

        //    return start;
        //}
    }
}
